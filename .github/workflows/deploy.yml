# # .github/workflows/deploy.yml
# name: Deploy
# on:
#   push:
#     branches: [ main ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2

#       - name: Set up Python
#         uses: actions/setup-python@v2
#         with:
#           python-version: '3.8'  # Thiết lập phiên bản Python

#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt  # Cài đặt các thư viện cần thiết

#       # - name: Deploy to DO
#       #   uses: appleboy/ssh-action@master
#       #   with:
#       #     host: ${{ secrets.DROPLET_IP }}
#       #     username: deploy
#       #     key: ${{ secrets.SSH_PRIVATE_KEY }}
#       #     script: |
#       #       cd ~/app
#       #       git pull
#       #       docker-compose up -d --build

#       - name: Deploy to DigitalOcean
#         run: |
#           ssh root@<địa_chỉ_IP> 'cd /path/to/your/project && git pull && pip install -r requirements.txt && python manage.py migrate && systemctl restart gunicorn'
#         env:
#           SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}  # Sử dụng khóa SSH để truy cập




name: Django CI/CD

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r noxa_be/requirements.txt

    
    - name: SSH to server and deploy
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd Noxa-tutor-be/noxa-be
          git pull origin deploy
          source venv/bin/activate  # Nếu sử dụng virtualenv
          pip install -r requirements.txt
          python manage.py migrate
          python manage.py collectstatic --noinput
          sudo systemctl restart scrum.service
          sudo systemctl restart nginx
          # systemctl restart gunicorn
          # systemctl restart nginx
